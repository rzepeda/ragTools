# =============================================================================
# RAG Factory - Test Environment Configuration
# =============================================================================
# Copy this file to tests/.env.test and update with your test database credentials
#
# This file is specifically for test configuration and is loaded by pytest.
# =============================================================================

# =============================================================================
# Test Database Configuration
# =============================================================================

# Test database URL (required)
# Format: postgresql://user:password@host:port/database_name
TEST_DATABASE_URL=postgresql://test_user:test_password@localhost:5432/rag_test

# Alternative: Individual connection parameters
# If you prefer to set individual parameters instead of a URL:
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=rag_test
# DB_USER=test_user
# DB_PASSWORD=test_password

# For VM development (accessing database on host machine):
# HOST_IP=192.168.56.1
# TEST_DATABASE_URL=postgresql://rag_user:rag_password@${HOST_IP}:5432/rag_test

# =============================================================================
# Test Execution Flags
# =============================================================================

# Database tests (requires PostgreSQL with pgvector extension)
RUN_DB_TESTS=true

# Integration tests (requires all services to be available)
RUN_INTEGRATION_TESTS=true

# LLM tests (requires API keys - usually disabled in test environments)
RUN_LLM_TESTS=false

# =============================================================================
# Optional: Test Service Configuration
# =============================================================================

# Neo4j for graph tests (optional)
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=test_password

# For VM development:
# NEO4J_URI=bolt://${HOST_IP}:7687

# API keys for integration tests (optional)
# Only needed if RUN_LLM_TESTS=true
# OPENAI_API_KEY=sk-test-key
# COHERE_API_KEY=test-key
# LM_STUDIO_BASE_URL=http://localhost:1234/v1

# =============================================================================
# Test Setup Guide
# =============================================================================
#
# 1. Create test database:
#    createdb rag_test
#    psql rag_test -c "CREATE EXTENSION vector;"
#
# 2. Copy this file:
#    cp tests/.env.test.example tests/.env.test
#
# 3. Update TEST_DATABASE_URL with your credentials
#
# 4. Run tests:
#    pytest tests/ -v
#
# 5. Run specific test categories:
#    pytest tests/unit/ -v                    # Unit tests only
#    pytest tests/integration/database/ -v    # Database integration tests
#
# See docs/database/ENVIRONMENT_VARIABLES.md for detailed documentation
# =============================================================================
