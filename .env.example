# =============================================================================
# RAG Factory - Environment Configuration Template
# =============================================================================
# Copy this file to .env and update with your configuration
#
# This template provides examples for all supported environment variables.
# Uncomment and modify the variables you need for your setup.
# =============================================================================

# =============================================================================
# Host IP Configuration (for VM development)
# =============================================================================
# If running in a VM and accessing services on the host machine, set HOST_IP
# Common values:
#   - 10.0.2.2        (VirtualBox NAT - most common)
#   - 192.168.56.1    (VirtualBox Host-Only)
#   - 192.168.122.1   (KVM/QEMU)
#   - 172.16.0.1      (VMware NAT)
# To find yours: ip route | grep default
HOST_IP=192.168.56.1

# =============================================================================
# Database Configuration
# =============================================================================

# Main database URL (required)
# Format: postgresql://user:password@host:port/database_name
DATABASE_URL=postgresql://rag_user:rag_password@localhost:5432/rag_factory

# Test database URL (required for running tests)
# IMPORTANT: Use TEST_DATABASE_URL (not DATABASE_TEST_URL)
TEST_DATABASE_URL=postgresql://rag_user:rag_password@localhost:5432/rag_test

# For VM development (accessing database on host machine):
# DATABASE_URL=postgresql://rag_user:rag_password@${HOST_IP}:5432/rag_factory
# TEST_DATABASE_URL=postgresql://rag_user:rag_password@${HOST_IP}:5432/rag_test

# =============================================================================
# Neo4j Configuration (Optional)
# =============================================================================
# Uncomment if using Neo4j for graph-based features

# NEO4J_URI=bolt://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=rag_password

# For VM development:
# NEO4J_URI=bolt://${HOST_IP}:7687

# =============================================================================
# LLM Configuration (Optional)
# =============================================================================

# --- OpenAI ---
# OPENAI_API_KEY=sk-your-key-here
# OPENAI_MODEL=gpt-4
# OPENAI_API_BASE=https://api.openai.com/v1  # Optional: custom endpoint

# --- Cohere ---
# COHERE_API_KEY=your-key-here

# --- Local LM Studio (OpenAI-compatible) ---
# LM_STUDIO_BASE_URL=http://localhost:1234/v1
# LM_STUDIO_MODEL=local-model

# For VM development (LM Studio on host):
# LM_STUDIO_BASE_URL=http://${HOST_IP}:1234/v1
# OPENAI_API_BASE=http://${HOST_IP}:1234/v1
# OPENAI_API_KEY=lm-studio  # Any value works with LM Studio

# =============================================================================
# Embedding Models (Optional)
# =============================================================================

# ONNX models (local)
# ONNX_MODEL_PATH=/path/to/models

# HuggingFace cache directory
# HF_HOME=~/.cache/huggingface

# =============================================================================
# Test Execution Flags
# =============================================================================

# Enable/disable test categories
RUN_DB_TESTS=true
RUN_INTEGRATION_TESTS=true
RUN_LLM_TESTS=false  # Set to true if you have API keys configured

# =============================================================================
# Development Settings
# =============================================================================

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Python path (if needed)
# PYTHONPATH=./src

# =============================================================================
# Quick Setup Guide
# =============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Update DATABASE_URL and TEST_DATABASE_URL with your credentials
#
# 3. (Optional) Configure LLM providers if needed
#
# 4. Test your configuration:
#    pytest tests/ -v
#
# For VM development:
# 1. Find your host IP: ./find-host-ip.sh
# 2. Update HOST_IP above
# 3. Use ${HOST_IP} in connection URLs
#
# See docs/database/ENVIRONMENT_VARIABLES.md for detailed documentation
# =============================================================================
